#ifndef STRING_UTILS_H_SENTRY
#define STRING_UTILS_H_SENTRY

extern const int alphabet_size;

int is_lower_case_letter(char c);

int is_upper_case_letter(char c);

int convert_str_to_int(char* str, int* shift);

#endif

